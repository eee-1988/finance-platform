apiVersion: apps/v1
kind: Deployment
metadata:
  name: finance-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
        - name: worker
          image: python:3.11
          command: ["python","worker.py"]